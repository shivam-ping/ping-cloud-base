apiVersion: apps/v1
kind: Prometheus
metadata:
  name: k8s
  namespace: prometheus
spec:
  containers:
    - name: prometheus
      resources:
        limits:
          cpu: “2000m”
          memory: "6000Mi"
        requests:
          cpu: “1500m”
          memory: "4000Mi"
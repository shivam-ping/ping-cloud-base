apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus
  namespace: prometheus
spec:
  resources:
    limits:
      cpu: “2000m”
      memory: "6000Mi"
    requests:
      cpu: “1500m”
      memory: "4000Mi"
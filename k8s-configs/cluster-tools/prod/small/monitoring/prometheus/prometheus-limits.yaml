apiVersion: apps/v1
kind: Prometheus
metadata:
  name: k8s
  namespace: prometheus
spec:
  containers:
    - name: prometheus
      resources:
        limits:
          cpu: “1000m”
          memory: "3000Mi"
        requests:
          cpu: “100m”
          memory: "1500Mi"
apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus
  namespace: prometheus
spec:
  resources:
    limits:
      cpu: “1500m”
      memory: "4000Mi"
    requests:
      cpu: “1000m”
      memory: "2000Mi"